# The Terms page used to publish under /tos/. Shipped app builds, App Store
# listings and anything linked from outside still point there, and those links
# outlive the rename — so the old slug keeps resolving instead of 404ing.
/tos/ /terms/ 301
/:lang/tos/ /:lang/terms/ 301
