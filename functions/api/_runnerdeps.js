export const RUNNER_DEPS_PY = "import ast\nimport importlib.util\nimport os\nimport subprocess\nimport sys\n\nALIASES = {\n    \"bs4\": \"beautifulsoup4\", \"PIL\": \"pillow\", \"cv2\": \"opencv-python-headless\", \"sklearn\": \"scikit-learn\",\n    \"skimage\": \"scikit-image\", \"yaml\": \"pyyaml\", \"dateutil\": \"python-dateutil\", \"dotenv\": \"python-dotenv\",\n    \"Crypto\": \"pycryptodome\", \"jwt\": \"PyJWT\", \"docx\": \"python-docx\", \"pptx\": \"python-pptx\",\n    \"fitz\": \"pymupdf\", \"serial\": \"pyserial\", \"git\": \"GitPython\", \"attr\": \"attrs\", \"OpenSSL\": \"pyOpenSSL\",\n    \"websocket\": \"websocket-client\", \"psycopg2\": \"psycopg2-binary\", \"MySQLdb\": \"mysqlclient\", \"zmq\": \"pyzmq\",\n    \"slugify\": \"python-slugify\", \"jose\": \"python-jose\", \"multipart\": \"python-multipart\", \"telegram\": \"python-telegram-bot\",\n    \"discord\": \"discord.py\", \"mpl_toolkits\": \"matplotlib\", \"Levenshtein\": \"python-Levenshtein\", \"magic\": \"python-magic\",\n    \"google\": None, \"_typeshed\": None, \"__future__\": None,\n}\n\n\ndef imported(path):\n    try:\n        with open(path, encoding=\"utf-8\") as f:\n            tree = ast.parse(f.read(), path)\n    except (OSError, SyntaxError, ValueError):\n        return []\n    names = []\n    for node in ast.walk(tree):\n        if isinstance(node, ast.Import):\n            names += [a.name.split(\".\")[0] for a in node.names]\n        elif isinstance(node, ast.ImportFrom) and node.level == 0 and node.module:\n            names.append(node.module.split(\".\")[0])\n    return names\n\n\ndef local(name, here):\n    return os.path.exists(os.path.join(here, name + \".py\")) or os.path.isdir(os.path.join(here, name))\n\n\ndef missing(names, here):\n    out = []\n    for name in dict.fromkeys(names):\n        if not name or name in sys.stdlib_module_names or name in sys.builtin_module_names or local(name, here):\n            continue\n        if name in ALIASES and ALIASES[name] is None:\n            continue\n        if importlib.util.find_spec(name) is not None:\n            continue\n        out.append(ALIASES.get(name, name))\n    return list(dict.fromkeys(out))\n\n\ndef main():\n    here = os.getcwd()\n    names = []\n    for path in sys.argv[1:]:\n        names += imported(path)\n    need = missing(names, here)\n    if not need:\n        return 0\n    print(\"Installing \" + \", \".join(need) + \"...\", file=sys.stderr, flush=True)\n    env = dict(os.environ, PIP_BREAK_SYSTEM_PACKAGES=\"1\", PIP_DISABLE_PIP_VERSION_CHECK=\"1\")\n    pip = [sys.executable, \"-m\", \"pip\", \"install\", \"-q\", \"--no-warn-script-location\"]\n    if subprocess.call(pip + need, env=env, stderr=subprocess.DEVNULL) != 0:\n        for name in need:\n            if subprocess.call(pip + [name], env=env) != 0:\n                print(\"Could not install \" + name + \".\", file=sys.stderr, flush=True)\n    return 0\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())\n";

export const RUNNER_DEPS_MJS = "import { readFileSync, existsSync } from \"node:fs\";\nimport { builtinModules } from \"node:module\";\nimport { spawnSync } from \"node:child_process\";\n\nconst SPEC = /(?:\\bimport\\s+(?:type\\s+)?(?:[\\w*{}\\s,$]+\\s+from\\s+)?|\\bimport\\s*\\(\\s*|\\brequire\\s*\\(\\s*|\\bexport\\s+(?:type\\s+)?[\\w*{}\\s,$]+\\s+from\\s+)[\"']([^\"'\\n]+)[\"']/g;\nconst BUILTIN = new Set(builtinModules.flatMap((m) => [m, m.replace(/^node:/, \"\")]));\n\nfunction packageOf(spec) {\n  if (!spec || spec.startsWith(\".\") || spec.startsWith(\"/\") || /^[a-z]+:/i.test(spec)) return null;\n  const parts = spec.split(\"/\");\n  const name = spec.startsWith(\"@\") ? parts.slice(0, 2).join(\"/\") : parts[0];\n  if (!/^(@[a-z0-9][\\w.-]*\\/)?[a-z0-9][\\w.-]*$/i.test(name)) return null;\n  if (BUILTIN.has(name) || BUILTIN.has(spec)) return null;\n  return name;\n}\n\nconst need = new Set();\nfor (const file of process.argv.slice(2)) {\n  let text = \"\";\n  try { text = readFileSync(file, \"utf8\"); } catch { continue; }\n  text = text.replace(/^\\s*(?:import|export)\\s+type\\b[^\\n]*$/gm, \"\");\n  for (const m of text.matchAll(SPEC)) {\n    const name = packageOf(m[1]);\n    if (name && !existsSync(\"node_modules/\" + name + \"/package.json\")) need.add(name);\n  }\n}\nif (need.size) {\n  const list = [...need];\n  process.stderr.write(\"Installing \" + list.join(\", \") + \"...\\n\");\n  const npm = (names, quiet) => spawnSync(\"npm\", [\"install\", \"--no-save\", \"--no-audit\", \"--no-fund\", \"--loglevel=error\", ...names],\n    { stdio: [\"ignore\", \"ignore\", quiet ? \"ignore\" : \"inherit\"] }).status === 0;\n  if (!npm(list, true)) {\n    for (const name of list) {\n      if (!npm([name], false)) process.stderr.write(\"Could not install \" + name + \".\\n\");\n    }\n  }\n}\n";
