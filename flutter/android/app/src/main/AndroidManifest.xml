<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Core connectivity: relay sockets and the Nymbot worker. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Dictation into the composer. Nothing is recorded: the platform's own
         recogniser turns speech into text and the app only sees the text. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Android 11 package visibility: without these the speech recogniser and
         the text-to-speech engine are invisible to the app even when installed,
         and both silently report themselves unavailable. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE"/>
        </intent>
    </queries>

    <!-- Backups are fully disabled: Auto Backup would copy the conversation
         store and SharedPreferences to the user's Google account, and adb
         backup could extract the same data locally. allowBackup=false disables
         both; the rule files are belt-and-braces for agents that consult rules
         despite the flag. Identity secrets already live in the Keystore. -->
    <application
        android:label="Nymbot"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <!-- url_launcher: pay a credit invoice in a Lightning wallet. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="lightning"/>
        </intent>
        <!-- url_launcher: open a link a reply cited. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
